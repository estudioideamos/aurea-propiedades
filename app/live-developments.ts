import type { Development } from "./developments";
import { developments as staticDevelopments, formatDevelopmentPrice } from "./developments";
function parse(value: unknown): string[] { if(Array.isArray(value))return value.map(String); if(typeof value!=="string")return []; try{return JSON.parse(value) as string[];}catch{return [];} }
export async function getLiveDevelopments(): Promise<Development[]> {
  if(process.env.GITHUB_PAGES==="true")return staticDevelopments;
  try { const [{desc,eq},{getDb},{developmentRecords},{refreshTokkoIfDue}]=await Promise.all([import("drizzle-orm"),import("../db"),import("../db/schema"),import("./tokko-integration")]); await refreshTokkoIfDue(); const rows=await getDb().select().from(developmentRecords).where(eq(developmentRecords.publicationStatus,"published")).orderBy(desc(developmentRecords.updatedAt)); if(!rows.length)return staticDevelopments; return rows.map(row=>{const item:Development={id:row.id,slug:row.slug,title:row.title,location:row.location,neighborhood:row.neighborhood,status:row.status,delivery:row.delivery,units:row.units,currency:row.currency as "USD"|"ARS",priceValue:row.priceValue,pricePrefix:row.pricePrefix,priceSuffix:row.priceSuffix,price:"",image:row.image,gallery:parse(row.gallery),floors:row.floors,apartments:row.apartments,garages:row.garages,developer:row.developer,architect:row.architect,description:parse(row.description),amenities:parse(row.amenities),specifications:parse(row.specifications),publicationStatus:"published",source:row.source as "manual"|"tokko",externalId:row.externalId,externalUpdatedAt:row.externalUpdatedAt,updatedAt:row.updatedAt};item.price=formatDevelopmentPrice(item);return item;}); } catch { return staticDevelopments; }
}
export async function getLiveDevelopment(slug:string){const all=await getLiveDevelopments();return{development:all.find(item=>item.slug===slug),all};}
